# Navigations and Automappings
alias h='cd "$HOME"'
alias hc='cd "$HOME" && clear'
alias c='clear'
alias cc='claude'
alias ..='cd ..'

### zoxide (smart cd)
eval "$(zoxide init zsh)"
eval $(thefuck --alias)
